services:
  - type: web
    name: attendx
    runtime: python

    plan: free

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      gunicorn -k eventlet -w 1 run:app

    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9

      - key: RENDER
        value: "true"

    disk:
      name: attendx-data
      mountPath: /var/data
      sizeGB: 5